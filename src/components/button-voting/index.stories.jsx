import ButtonVoting from '.';

export default {
  title: 'atoms/ButtonVoting',
  component: ButtonVoting,
};

const Template = (args) => <ButtonVoting {...args} />;

export const Default = Template.bind({});

Default.args = {};
